import React from "react";

import { FormContainer } from "./form-component.styles";

const FormComponent = ({ children }) => (
  <FormContainer>{children}</FormContainer>
);

export default FormComponent;
